# Project Definition & AI Agent Guidelines: SKT ALEPH Real Information Board (과제 4: 오늘의 진짜 정보판 — 데이터가 안 올 때)

이 문서는 본 프로젝트의 핵심 품질 기준, 웹 접근성, 레이아웃 규격 및 과제 통과 기준(T04-C01 ~ T04-C35)을 영구적으로 정의하고 강제하는 프로젝트 정의 파일(Rules Specification)입니다.
향후 모든 코드 수정, 신규 기능 추가 및 스타일 변경 시 본 문서에 명시된 규칙을 반드시 준수해야 합니다.

---

## 1. 기본 아키텍처 & 디자인 원칙

- **무로그인 공개 정적 웹 (T04-C01, T04-C02, T04-C29 ~ T04-C33)**: 계정 생성, 로그인, 인증, 초대, 비밀번호, OAuth, CAPTCHA 없이 브라우저 새 시크릿 창에서 누구나 즉시 정보판을 확인하고 테스트할 수 있어야 합니다.
- **모노크롬 & 글래스모피즘 스타일**: 과도한 원색 배경을 지양하고, 다크/라이트 모드를 모두 지원하는 정돈된 글래스모피즘(`backdrop-blur`, 은은한 보더, 미세 광원 효과)을 유지합니다.
- **품질 자동화**: 코드를 변경한 후에는 반드시 다음 검증을 통과해야 합니다.
  - `npm run format`: Prettier 포맷팅 준수
  - `npm run lint`: Oxlint 오류 및 경고 0건
  - `npm run build`: TypeScript 컴파일 및 Vite 번들링 0 오류

---

## 2. 헤더 고정 & 레이아웃 규격 (Header & Layout Constraints)

- **헤더 상단 스크롤 고정 (Fixed Header)**:
  - Header는 스크롤 동작과 무관하게 뷰포트 최상단에 고정되어야 합니다 (`fixed top-0 inset-x-0 z-50`).
  - 반투명 배경 및 블러(`bg-white/90 backdrop-blur-md border-b border-neutral-200/80 dark:bg-neutral-900/90 dark:border-neutral-800`)로 스크롤 시 하단 콘텐츠가 비치되 네비게이션 가독성을 보장해야 합니다.
- **콘텐츠 패딩 및 앵커 오프셋 보장**:
  - `<main>` 컨테이너는 헤더 높이(`h-16`, 64px)를 감안하여 상단 패딩(`pt-20 sm:pt-24`)을 반드시 유지하여 콘텐츠가 헤더에 가려지지 않아야 합니다.
  - 내부 섹션 앵커(`id="board"`, `id="synthetic-runner"`, `id="daily-history"`, `id="criteria-audit"`)는 상단 고정 헤더에 제목이 겹치지 않도록 `scroll-mt-24` 이상의 오프셋을 유지해야 합니다.
- **PC 경계 해상도 조작 영역 잘림 및 가로 넘침 0건**:
  - **1366×768** 및 **1920×1080** 해상도에서 사용자가 스크롤을 내리지 않아도 첫 화면에 핵심 정보판(값, 단위, 출처, 두 시각, 기준 시간대, 상태 배지)이 잘림 없이 100% 들어와야 합니다.
  - 최상위 컨테이너에 `overflow-x-clip` 및 반응형 패딩을 적용하여 어떠한 해상도에서도 가로 스크롤이 발생하지 않아야 합니다.

---

## 3. 카드 1: 매일 궁금한 값 하나 (T04-C01 ~ T04-C10, T04-C25, T04-C26)

- **무로그인 공개 열람 (T04-C01, T04-C02)**: 계정 생성 및 로그인 없이 시크릿 창에서 정상 작동.
- **비개인 공개 원천 실제 동적 값 조회 (T04-C03)**: 실제 비개인 공개 원천(서울 기상청/Open-Meteo 실시간 기온 등)에서 동적 값을 실시간 조회 가능.
- **화면 필수 메타데이터 6종 상시 표시 (T04-C04 ~ T04-C09)**:
  - 값 (T04-C04): 실시간 관측값 표시.
  - 단위 (T04-C05): 측정 단위(예: `°C`, `pt` 등) 명시.
  - 출처 (T04-C06): 데이터 출처 이름 및 출처 URL 명시.
  - 출처 시각 (T04-C07): 원천 데이터 관측 시각(source_time) 표시.
  - 조회 시각 (T04-C08): 실제 API 호출/수집 시각(fetched_at) 표시.
  - 기준 시간대 (T04-C09): `Asia/Seoul` (KST, UTC+9) 명시.
- **정상 1건 일치성 (T04-C10)**: 원자료(API 응답 원문), 저장값(DB/localStorage), 화면 표시값이 100% 일치.
- **개인정보 0건 (T04-C25)**: 공개 심사 화면과 제출 파일에 실제 개인정보/개인 기록 0건.
- **합성 시험값 격리 (T04-C26)**: 5종 실패 재현에는 합성 fixture 시험값만 사용.

---

## 4. 카드 2: 비밀 없는 호출 (T04-C11)

- **비밀키 원문 0건 (T04-C11)**: 브라우저 코드, 빌드 배포 파일, 네트워크 요청/응답, Git 기록에 API Key나 비밀값 원문이 일체 포함되지 않아야 합니다 (완전 무키 공개 API 사용).

---

## 5. 카드 3: 다섯 가지 실패 & 회복 규격 (T04-C12 ~ T04-C19)

공식 공개 9종 fixture(`assets/t04-real-information-board/fixtures`)를 내장하여 결정론적 상태 전이를 보장합니다:

- **느린 외부 응답 (T04-C12)**: `T04-TIMEOUT` -> stale / timeout 상태 표시.
- **외부 401/403 거절 (T04-C13)**: `T04-AUTH-401` -> stale / auth 상태 표시 (외부 원천 오류 안내).
- **호출 제한 (T04-C14)**: `T04-RATE-429` -> stale / rate_limit 상태 및 Retry-After 관측값 안내.
- **오프라인 (T04-C15)**: `T04-OFFLINE` -> stale / offline 상태 표시.
- **응답 형식 변경 (T04-C16)**: `T04-SCHEMA-BREAK` -> stale / schema_error 상태 표시.
- **마지막 정상값 보존 (T04-C17)**: 어떤 실패가 발생해도 직전 정상값(예: 105)이 지워지거나 null 처리되지 않고 화면에 유지.
- **오래된 값(Stale) 표시 (T04-C18)**: 실패 뒤 마지막 정상값에 '오래된 값 (Stale)' 배지와 안내가 표시됨.
- **다시 시도 행동 및 회복 (T04-C19)**: 실패 상태에서 '다시 시도' 버튼이 보이며, `T04-RECOVER-D2` 재생 시 fresh / none으로 복구되고 다음 날짜(`2026-08-25`) 일별 기록이 정확히 1건 추가됨(총 2행, 저장값 120, delta 15).

---

## 6. 카드 4: 하루 한 줄 & 날짜 키 (T04-C20 ~ T04-C21)

- **기준 시간대 날짜 키**: `record_timezone: "Asia/Seoul"` 기준의 날짜(`record_date: YYYY-MM-DD`)를 키로 사용.
- **동일 날짜 재실행 시 단일 행 유지 (T04-C20)**: 같은 Asia/Seoul 날짜에 여러 번 성공해도 일별 기록은 1건으로 합쳐져 원자적 갱신(Update).
- **익일 성공 시 신규 행 추가 (T04-C21)**: Asia/Seoul 기준 다음 날짜에 성공 시 새 일별 기록이 생성(Create).

---

## 7. 카드 5: 실제 이틀과 어제 대비 & 제출 규격 (T04-C22 ~ T04-C35)

- **서로 다른 KST 2일 실제 기록 보존 (T04-C22)**: 서로 다른 Asia/Seoul 실제 날짜에 조회한 비개인 공개 원천 기록이 정확히 2건 보존되어 있어야 함.
- **원천·저장·화면 일치 (T04-C23)**: 두 기록 각각의 URL, 관측 시각, 정규화 값, 단위가 저장값 및 화면 표시값에서 100% 일치.
- **어제 대비 변화 재계산 일치 (T04-C24)**: 두 기록을 조회 날짜순으로 정렬하여 `오늘 값 - 어제 값` 공식으로 다시 계산한 변화값이 화면의 어제 대비 변화값과 100% 일치.
- **짧은 확인 방법 4줄 (T04-C27)**: ① 어디로 가나요, ② 3단계 이내 무엇을 하나요, ③ 무엇이 보이면 통과인가요, ④ 안 될 때 무엇이 보이나요가 명확히 구분되어 수록.
- **AI와 나의 판단 3줄 (T04-C28)**: ① AI에게 맡긴 일, ② 학생이 직접 판단한 일, ③ AI 제안을 따르지 않은 일(없으면 이유)이 명확히 구분되어 수록.
- **공개 접근성 보장 (T04-C29 ~ T04-C33)**: 인증, 초대, 비밀번호, OAuth, CAPTCHA 0건.
- **제출 형식 (T04-C34, T04-C35)**: 결과물 HTTPS URL 및 커밋 해시(40/64자리 hex)가 포함된 소스 HTTPS URL 제출 가이드 제공.
